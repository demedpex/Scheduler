# Claude Design 프롬프트 — 업무 스케줄러 UI 개편

이 문서는 두 부분입니다.

1. **Claude Design 에 그대로 붙여넣을 프롬프트** (아래 `---` 안쪽 전체)
2. **디자인 결과를 Claude Code 로 다시 가져오는 방법**

---

## 1. 붙여넣을 프롬프트

> 아래 코드블록 전체를 복사해 Claude Design 에 붙여넣으세요.

````text
사내 내부망에서 도는 "업무 스케줄러" 웹앱의 UI를 개편하려고 합니다.
단일 HTML 파일로 이미 동작 중이고, 로직은 그대로 두고 화면만 다시 디자인합니다.

## 이 도구의 성격

- 사용자: 같은 팀 2~5명. 사무직. 하루에 몇 번 열어보는 업무 도구입니다.
- 화려함보다 **한눈에 빠르게 훑히는 것**이 중요합니다. 대시보드형 밀도.
- 인터넷이 끊긴 내부망 PC에서 브라우저로 여는 단일 .html 파일입니다.
- 한국어 UI. 모든 문구는 존댓말.

## 절대 지켜야 할 제약 (어기면 못 씁니다)

- **외부 리소스 일절 금지.** 웹폰트(Google Fonts 포함), CDN, 아이콘 라이브러리,
  외부 이미지 전부 안 됩니다. 인터넷이 없습니다.
- 폰트는 **윈도우 기본 한글 폰트만**: `"Malgun Gothic","맑은 고딕",-apple-system,"Segoe UI",sans-serif`
- 아이콘이 필요하면 **유니코드 문자**(◀ ▶ ☐ ☑ ● ✓ ─) 또는 **인라인 SVG**로만.
- 데스크톱 전용. 모바일 레이아웃은 필요 없습니다. **기준 폭 1440px** (1280~1920 대응).
- "오늘" 화면은 **인쇄**가 되어야 합니다. 인쇄 시 버튼·탭·네비게이션은 숨김.
- 사내 PC라 최신 브라우저가 아닐 수 있습니다. 실험적 CSS는 피하고 폴백을 두세요.

## ★ 이번 개편의 방향 (가장 중요)

### 1) "업무"가 주인공입니다

지금은 업무 / 연락하기 / 업무공유 / 점심약속 네 섹션이 **똑같은 크기의 카드로
세로로 쌓여** 있습니다. 넷 다 동등해 보여서 뭐가 중요한지 안 보입니다.

- **업무**가 이 앱의 핵심입니다. 가장 크고, 가장 먼저, 가장 잘 보여야 합니다.
  항목이 10개쯤 되어도 스크롤 없이 한눈에 들어왔으면 합니다.
- **업무공유**는 회신기한(D-day) 압박이 있어서 놓치면 안 됩니다. 두 번째로 눈에 띄게.
- **유관부서 연락하기**, **점심약속**은 보조입니다. 자리는 있되 주역을 밀어내면 안 됩니다.

넷을 똑같이 나열하지 말고, **중요도가 크기와 위치로 드러나게** 재배치해 주세요.
2단 구성(넓은 주 영역 + 좁은 보조 열)이든, 업무를 상단 전폭에 두고 나머지 셋을
아래 3열로 두든, 더 나은 안이 있으면 제안해 주세요. 여러 안을 비교해 보여 주셔도 좋습니다.

### 2) PC 화면을 제대로 쓰세요

- 지금은 본문 최대 폭이 1080px이라 넓은 모니터에서 양옆이 텅 빕니다.
- **기준 폭 1440px**, 1280~1920px에서 자연스럽게 동작하도록 해 주세요.
- 사무실 모니터 기준입니다. 모바일·태블릿 레이아웃은 필요 없습니다.
- 가로 공간이 남으니 세로 스크롤을 줄이는 쪽으로 쓰면 좋겠습니다.

### 3) 상단 메뉴가 눈에 들어와야 합니다

- 지금 헤더와 탭(오늘 / 달력 / 설정)이 흐릿한 회색 텍스트라 잘 안 보입니다.
  어디에 있는지, 어디로 갈 수 있는지가 **즉시** 읽혀야 합니다.
- 헤더에는 앱 이름, 현재 위치, 저장 상태, [파일로 저장]이 함께 있습니다.
  이것들이 서로 안 묻히게 위계를 잡아 주세요.
- 특히 **"파일로 저장하지 않은 변경 있음"** 경고는 놓치면 데이터를 잃는 상태입니다.
  지금은 작은 빨간 글씨라 잘 안 보입니다. 확실히 보이되 계속 거슬리지는 않게.

---

## 화면 구성

상단에 고정 헤더(앱 이름 + 저장 상태 + [파일로 저장] 버튼), 그 아래 탭 3개
(**오늘 / 달력 / 설정**), 그 아래 본문입니다.

### 화면 1 — "오늘" (가장 많이 보는 화면)

날짜 이동 바: `◀  2026-08-27 (수) · 오늘  ▶  [오늘로]` +
등록 버튼 4개(업무 / 연락 / 공유 / 점심).

내용은 네 덩어리입니다. **배치와 크기는 위 "개편의 방향"대로 다시 잡아 주세요.**
아래는 각 덩어리에 무엇이 들어가는지입니다.

1. **업무** — 체크박스 / 제목 / 부제(내용 앞부분) / 시간(우측).
   완료하면 취소선 + 회색. 마우스 올리면 [수정] 버튼이 나타남.
2. **유관부서 연락하기** — 체크박스 / 이름(부서) / `내선 1234` / 시간.
   명부에 없으면 `(명부 없음)`.
3. **업무공유** — D-day 배지 / 제목 / `문서번호 · 회신기한 09/02 · 작성자`.
   섹션 제목 오른쪽에 [공유 내보내기] [공유 가져오기] 버튼.
4. **점심약속** — 대상 이름 / 장소 / 시간.

**D-day 배지 4단계** (이게 시각적으로 제일 중요합니다):
- `D-DAY` — 오늘이 기한. 가장 강한 강조
- `D+2` — 기한 지남. 경고
- `D-3` — 3일 이내. 주의
- `D-10` — 여유. 차분하게

빈 섹션은 "등록된 업무가 없습니다." 같은 안내 한 줄.

### 화면 2 — "달력"

`◀ 2026년 8월 ▶ [이번 달로]` + 7열 × 6행 격자.

각 칸: 날짜 숫자(일요일 빨강 / 토요일 파랑 / 이번 달 아닌 날 흐리게) +
그 아래 작은 글씨로 `업무 3 · 연락 1`, `공유 2 · 점심 1`.

- 오늘 칸: 뚜렷하게 강조
- 회신기한이 3일 내인 공유건이 있는 날: 다르게 표시(빨간 계열)
- 칸을 누르면 그 날짜의 "오늘" 화면으로 이동

### 화면 3 — "설정"

카드 5개: 내 정보 / 직원 명부(파일 올리기) / 업무공유(조각당 글자 수) /
데이터(백업·내보내기·전체삭제) / 도움말(설명 문단).

`라벨 — 값/입력` 형태의 행이 반복됩니다.

### 모달 (팝업)

1. **업무·연락·공유·점심 등록/수정** — 입력 폼. 하단 [삭제] [취소] [저장]
2. **동명이인 선택** — 명부에서 이름이 겹칠 때 `이름 / 부서 / 직급 / 내선` 목록
3. **내용 보기** — 긴 텍스트 읽기용
4. **공유 내보내기** ★ 아래 설명 참고
5. **공유 가져오기** ★ 아래 설명 참고

## ★ 가장 공들여야 할 부분: 조각 복붙 흐름

이 앱의 제일 어려운 부분이고, 디자인으로 가장 크게 개선될 곳입니다.

사내 보안정책상 **파일 첨부가 안 되고 공유폴더도 없어서**, 업무공유 내용을
**메신저 대화창에 텍스트로 붙여넣어** 주고받습니다. 메신저가 한 번에 800자밖에
못 받아서 내용이 길면 여러 조각으로 나눠 **여러 번 복사-붙여넣기**를 반복해야 합니다.

- **내보내기**: 범위 선택 → "대상 5건 · 조각 3개" → [1/3 복사] 누르고 메신저에
  붙여넣기 → [2/3 복사] → ... 반복. 지금 몇 번째인지, 몇 개 남았는지가
  **한눈에** 보여야 하고, 다음에 뭘 눌러야 하는지 헷갈리면 안 됩니다.
- **가져오기**: 큰 텍스트 상자에 붙여넣으면 즉시 자동 인식. 조각을 받을 때마다
  진행이 채워집니다. **순서 상관없이** 받을 수 있어서, 어느 조각이 비었는지
  보여야 합니다(예: 1✓ 2 3✓ → "2번 조각이 없습니다"). 다 받으면 아래에
  `신규 / 갱신 / 변화없음 / 삭제` 딱지가 붙은 미리보기 목록이 나오고 [반영] 활성화.

이 두 흐름을 **진행 단계가 눈에 보이는 형태**로 디자인해 주세요.
지금은 작은 알약 배지를 나열하는 정도인데, 더 나은 방법이 있으면 제안해 주세요.

## 지금 디자인의 불만 (요약)

- 네 섹션이 동등한 카드로 쌓여 있어 **업무가 주인공으로 안 보입니다.**
- 본문 폭이 1080px이라 **넓은 모니터에서 양옆이 텅 비고** 세로만 깁니다.
- **상단 헤더와 탭이 흐려서** 눈에 안 들어옵니다.
- 저장 안 함 경고가 잘 안 보입니다. 놓치면 데이터를 잃습니다.
- 전반적으로 밋밋합니다. 업무 도구다운 절제된 완성도를 원합니다.

## 만들어 줄 아트보드

1. **오늘 — 기본.** 개편된 배치로. 업무 8건, 공유 3건(D-day 4단계 모두), 연락 2건,
   점심 1건이 들어간 상태. 1440px 폭.
2. **오늘 — 배치 대안.** 1번과 다른 배치안 하나 더. 비교해서 고르고 싶습니다.
3. 오늘 — 아무것도 없는 빈 상태
4. 달력 — 한 달치, 오늘 표시와 기한임박 표시가 보이게
5. 설정
6. 공유 내보내기 모달 — 3/5 조각 진행 중인 순간
7. 공유 가져오기 모달 — 5개 중 2개만 받아서 3개가 비어 있는 순간
8. 업무 등록 모달 + 동명이인 선택 모달
9. **헤더/탭 상세** — 기본 상태와 "저장 안 함" 경고가 떠 있는 상태 두 가지
10. **컴포넌트/토큰 시트** — 색 토큰, 타이포 스케일, 간격, 버튼 3종(기본/주요/위험),
    D-day 배지 4종, 상태 딱지 4종, 입력 필드, 목록 행(기본/완료/호버), 카드, 달력 칸

## 코드로 옮기기 쉽게 해 주세요 (중요)

개편한 디자인을 기존 HTML 파일에 다시 적용할 겁니다. 그래서:

- 색·간격·모서리·글자크기는 **`:root` 의 CSS 커스텀 속성(변수)** 으로 정의해 주세요.
  기존 파일이 이미 `--bg --panel --line --ink --accent --red --orange --green --radius`
  같은 이름을 쓰고 있으니, 가능하면 **같은 이름을 유지**해 주세요.
- 클래스 이름도 기존 것을 그대로 써 주세요:
  `.card .row .row.done .pill .pill.red .btn .btn.primary .btn.danger`
  `.cell .cell.today .datebar .field .frow .modal .backdrop .empty .hint`
- 새 컴포넌트가 필요하면 새 클래스를 만들되, 무슨 용도인지 이름에 드러나게 해 주세요.
- 10번 아트보드에는 **그 CSS 변수 정의 블록을 실제 코드로** 함께 보여 주세요.
- 레이아웃이 2단·3열 등으로 바뀌면 **그 격자 구조도 CSS(grid/flex)로** 적어 주세요.

이렇게 해 주시면 로직은 한 줄도 안 건드리고 스타일만 갈아끼울 수 있습니다.
````

---

## 2. 디자인 → Claude Code 로 가져오기

Claude Design 에서 만든 캔버스는 **Artifact 로 게시**됩니다.
**같은 계정이면 Claude Code 가 목록에서 알아서 찾습니다. URL 을 복사해 올 필요 없습니다.**

### 순서

1. Claude Design 에서 캔버스를 완성합니다. 그게 전부입니다.
   - **"공유(Share)" 는 안 눌러도 됩니다.** 공유는 *다른 사람*에게 열어주는 기능이고,
     본인 아티팩트를 Claude Code 가 읽는 데는 필요 없습니다.
     팀원에게 시안을 보여주려는 목적이라면 그때만 쓰세요.

2. Claude Code 로 돌아와 이렇게 말씀하시면 됩니다:

   ```
   디자인 캔버스대로 web/스케줄러.html 의 UI를 바꿔줘.
   로직은 건드리지 말고.
   ```

   제가 아티팩트 목록을 뒤져 캔버스를 찾습니다.
   여러 개라 헷갈리면 어느 것인지 되묻겠습니다.
   (원하시면 URL 을 같이 주셔도 됩니다. 더 확실하긴 합니다.)

3. 제가 하는 일:
   - 그 주소를 읽어 아트보드의 HTML/CSS 를 확인합니다
   - 10번 컴포넌트 시트에서 **CSS 변수와 컴포넌트 스타일을 추출**합니다
   - `web/스케줄러.html` 의 `<style>` 블록을 교체하고, 바뀐 배치에 맞게 마크업을 조정합니다
   - **JS 로직은 손대지 않습니다.** 다만 배치가 2단·3열로 바뀌면 화면을 그리는
     함수가 어느 컨테이너에 그리는지 정도는 조정이 필요할 수 있습니다
   - 검증 3종을 다시 돌려 아무것도 안 깨졌는지 확인합니다

### 안전장치

UI를 갈아엎어도 기능이 멀쩡한지 자동으로 확인할 수 있습니다. 개편 전후로
아래를 돌리면 됩니다.

```powershell
node build\xlsx-test.js      # 명부 읽기        17건
node build\interop.js        # 동기화 프로토콜  18건
node build\dday-test.js      # 날짜·D-day       9건
```

이 검사들은 **HTML 파일에서 코드를 직접 떼어내** 돌리기 때문에, 스타일만 바꿨다면
그대로 통과해야 합니다. 하나라도 깨지면 마크업을 건드리다 로직을 건드린 것입니다.

### 되돌리기

개편 전 파일을 남겨 두면 마음 편합니다.

```powershell
Copy-Item web\스케줄러.html web\스케줄러_개편전.html
```

### 주의할 점

- 캔버스를 **다른 계정이나 다른 워크스페이스**에서 만들면 목록에 안 뜹니다.
  그때는 URL 을 직접 주셔야 합니다.
- 디자인 결과에 **웹폰트나 외부 이미지가 섞여 있으면 그대로 못 씁니다.**
  제가 옮길 때 걸러내고 알려드리겠습니다.
- 아트보드는 정지 화면이라 **호버·포커스·비활성 상태**가 빠지기 쉽습니다.
  8번 컴포넌트 시트에 그 상태들을 넣어 달라고 한 이유입니다.
- 디자인이 데이터 밀도를 너무 낮추면(여백이 커지면) 한 화면에 안 들어옵니다.
  실제 데이터를 넣어보고 조정이 필요할 수 있습니다.
