# Claude Code 구현 프롬프트 — 내부망 업무 스케줄러

> 아래 `---` 아래 전체를 Claude Code에 그대로 붙여넣으세요.
> 프로젝트 폴더를 하나 만들고 그 안에서 `claude` 를 실행한 뒤 붙여넣으면 됩니다.
> 이 파일을 프로젝트 루트에 `SPEC.md` 로 저장해두고 "SPEC.md 읽고 구현해줘" 라고 해도 됩니다.

---

# 역할

너는 Excel VBA 전문 개발자다. 아래 명세대로 **망분리 내부망에서 동작하는 로컬 업무 스케줄러**를 구현한다.

# 절대 제약 (위반하면 전부 무효)

- 인터넷 없음. 외부 라이브러리·CDN·API 호출 일절 불가.
- 데이터베이스 없음. 데이터는 통합 문서 내 시트에만 저장.
- 네트워크 공유 폴더 없음. 사용자 간 데이터 교환은 **메신저 본문에 붙여넣는 텍스트**로만 가능. 첨부파일 불가.
- 외부 ActiveX 참조 금지: `MSCOMCTL.OCX`(ListView, TreeView 등), `Microsoft Scripting Runtime` 사전 바인딩 금지. `Dictionary`가 필요하면 `CreateObject("Scripting.Dictionary")` 후기 바인딩만 사용.
- 사용 인원 2~5명, 명부 수백~수천 행 규모.
- Excel 2016 이상 Windows 데스크톱 기준. Mac·웹 고려 불필요.

# 산출물 구조

```
프로젝트루트/
├─ src/                      VBA 소스 (텍스트, 버전관리 대상)
│   ├─ modMain.bas           진입점, 리본/버튼 핸들러, 초기화
│   ├─ modData.bas           시트 테이블 CRUD
│   ├─ modCalendar.bas       달력 렌더링
│   ├─ modToday.bas          오늘의 할일 렌더링
│   ├─ modEmployee.bas       명부 로드·검색
│   ├─ modSync.bas           ★ 텍스트 내보내기/가져오기/병합
│   ├─ modUtil.bas           ID 생성, 이스케이프, 체크섬, 클립보드, 로그
│   └─ frm*.frm              UserForm (아래 "폼 생성 방식" 참고)
├─ build/
│   └─ build.ps1             Scheduler.xlsm 를 처음부터 생성하는 빌드 스크립트
├─ dist/
│   └─ Scheduler.xlsm        빌드 결과물 (배포용)
├─ sample/
│   └─ 직원명부_양식.csv       명부 양식 샘플
└─ README.md                 설치·배포·사용 설명서 (한국어)
```

## 폼 생성 방식

`.frm` 파일은 `.frx` 바이너리 짝이 필요해 손으로 만들면 깨지기 쉽다. 따라서:

- **기본 방식**: `build/build.ps1` 이 Excel COM으로 빈 xlsm 을 만들고, 시트·표를 세팅하고, `src/*.bas` 를 `VBProject.VBComponents.Import` 하고, **UserForm 은 `VBComponents.Add(3)` 로 추가한 뒤 `.Designer.Controls.Add` 로 컨트롤을 코드로 배치**한다.
  - 전제: Excel → 옵션 → 보안 센터 → 매크로 설정 → **"VBA 프로젝트 개체 모델에 대한 액세스 신뢰" 체크**. README에 스크린샷 없이 경로만 명확히 안내할 것.
- **대체 방식(위 옵션이 정책상 잠겨 있을 때)**: 빌드 스크립트가 실패하면 `.bas` 파일만 두고, 사용자가 VBE에서 `파일 → 파일 가져오기` 로 수동 임포트하도록 안내. 이 경우 UserForm 대신 **시트 기반 입력 패널**로 동작하는 폴백 경로를 반드시 함께 구현한다.
- 빌드 스크립트는 실패 시 어떤 단계에서 왜 실패했는지 한국어로 출력할 것.

# 데이터 모델

모든 데이터 시트는 숨김 처리하고 `ListObject`(표)로 만든다. 표 이름은 아래 그대로.

## 공통 컬럼 (4개 업무 표 전부에 포함)

| 컬럼 | 타입 | 규칙 |
|---|---|---|
| `ID` | 문자열 | `작성자-yyyymmdd-hhnnss-4자리난수` 예: `홍길동-20260827-143052-8471`. **자동증가 숫자 절대 금지** (PC마다 따로 증가해 반드시 충돌) |
| `작성자` | 문자열 | 설정 시트의 내 이름 |
| `수정시각` | 문자열 | `yyyy-mm-dd hh:nn:ss`. 병합 시 최신 우선 판정 |
| `삭제여부` | 0/1 | 물리 삭제 금지. 플래그만 켠다(tombstone). 지운 항목이 병합 때 되살아나는 것을 막는 유일한 수단 |

## tbl_Task — 업무
`ID | 제목 | 내용 | 날짜 | 시간 | 완료여부 | 완료시각 | 작성자 | 수정시각 | 삭제여부`
- `시간`은 선택. 비어 있으면 그날 목록에서 시간 있는 항목들 뒤에 정렬.
- `날짜`가 오늘이 아니면 자동으로 그 날짜의 할일이 된다(별도 로직 불필요, 날짜 필터로 자연 처리).
- 로컬 전용. 동기화 대상 아님.

## tbl_Contact — 연락하기
`ID | 이름 | 부서 | 내선캐시 | 날짜 | 시간 | 완료여부 | 작성자 | 수정시각 | 삭제여부`
- 날짜·시간 모두 선택 항목.
- 내선번호는 **저장값이 아니라 명부에서 매번 조회한 결과**를 쓴다. `내선캐시`는 명부에 없는 사람일 때만 직접 입력한 값을 담는 예비 필드.
- 명부에 사번이 없으므로 조회 키는 `이름 + 부서`.
- 로컬 전용. 동기화 대상 아님.

## tbl_Share — 업무공유 ★동기화 대상
`ID | 문서번호 | 제목 | 내용 | 등록일 | 회신기한 | 작성자 | 수정시각 | 삭제여부`
- 문서번호 필수. 등록 시 중복 검사 후 경고(차단은 하지 않음).
- 회신기한 필수.

## tbl_Lunch — 점심약속
`ID | 대상이름 | 날짜 | 시간 | 장소 | 작성자 | 수정시각 | 삭제여부`
- 로컬 전용.

## mst_Employee — 직원 명부
`이름 | 내선번호 | 부서 | 직급`
- 사용자가 제공하는 CSV/XLSX 를 **"명부 갱신" 버튼**으로 읽어 이 시트에 통째로 복사한다. 외부 링크 수식 사용 금지(경로 바뀌면 전부 깨짐).
- 내선번호는 반드시 **텍스트 서식**으로 읽는다. 앞자리 0(`0212`)이 사라지면 안 된다.
- 부서·직급은 비어 있을 수 있다. 비어 있어도 전체 기능이 동작해야 한다.
- 중간 빈 행을 만나면 거기서 읽기를 멈춘다.

## cfg_Setting — 설정 (숨김)
`키 | 값` 형태. `내이름`, `명부파일경로`, `백업보관일수`, `마지막동기화시각` 등.

## log_Merge — 병합 로그 (숨김)
`시각 | 동작 | ID | 이전값요약 | 이후값요약 | 출처`
병합으로 덮어써진 내용은 여기에 남겨 나중에 확인 가능하게 한다.

# ★ 텍스트 동기화 프로토콜 (가장 중요)

메신저 본문에 복사·붙여넣기로만 데이터를 주고받는다. 이 부분이 이 프로그램의 핵심이며 가장 공들여 구현할 것.

## 포맷

```
###WSCH1|P1/3|N=5###
S|홍길동-20260827-143052-8471|기획-2026-0451|상반기 실적 보고|본문 첫줄\n둘째줄|2026-08-27|2026-08-30|홍길동|2026-08-27 14:30:52|0
S|홍길동-20260827-144210-2093|기획-2026-0452|예산 집행 계획|...|2026-08-27|2026-09-02|홍길동|2026-08-27 14:42:10|0
###END|P1/3|C=A3F1###
```

- 1행 헤더: 프로토콜 식별자 `WSCH1`, 파트 번호 `P현재/전체`, 이 파트의 레코드 수 `N=`.
- 본문: 한 줄에 레코드 하나. 접두사 `S` = tbl_Share 레코드.
- 마지막 행 푸터: 파트 번호 재확인 + 체크섬 `C=`.
- 필드 구분자는 `|`.

## 이스케이프 (반드시 이 순서대로)

인코딩 시:
1. `\` → `\\`
2. `|` → `\p`
3. CR(vbCr) → 제거, LF(vbLf) → `\n`
4. Tab → `\t`

디코딩은 역순으로 한 번의 문자 순회 파싱으로 처리한다(단순 `Replace` 역순 적용은 `\\p` 같은 입력에서 깨진다 — 문자를 앞에서부터 훑으며 `\` 를 만나면 다음 한 글자를 보고 판단하는 방식으로 구현할 것).

## 체크섬

파트 본문(헤더·푸터 제외) 전체 문자에 대해:
```
sum = 0
각 문자 c: sum = (sum * 31 + AscW(c)) Mod 65536
```
결과를 4자리 대문자 16진수로. 메신저가 문자를 변형했거나 복붙이 잘렸을 때 이걸로 잡는다.

## 분할 전송

메신저 글자수 제한 때문에 **파트당 약 1500자**를 넘지 않게 레코드 단위로 자른다.
- 레코드 하나가 1500자를 넘으면 그 레코드만 단독 파트로 보낸다(레코드 중간에서 자르지 않는다).
- 파트 수가 1개면 헤더에 `P1/1` 로 표기하고 그대로 동작한다.

## 내보내기 UI (`frmExport`)

1. 범위 선택: `전체` / `내가 등록한 것만` / `최근 N일` / `회신기한 미도래분만`
2. 미리보기: 대상 건수와 총 파트 수 표시
3. `[1/3 복사]` 버튼 → 해당 파트를 클립보드에 넣고 버튼 라벨이 `[2/3 복사]` 로 바뀜
4. 사용자는 메신저에 붙여넣고 → 다시 버튼 → 반복
5. 파트가 1개면 버튼 하나로 끝

클립보드는 `New MSForms.DataObject` 의 `.SetText` + `.PutInClipboard` 사용(UserForm 이 있으면 참조가 이미 걸려 있다).

## 가져오기 UI (`frmImport`) — "붙여넣으면 자동으로"

1. 큰 여러 줄 TextBox 하나 + 상태 라벨 + 미리보기 목록 + `[반영]` 버튼
2. **TextBox_Change 이벤트에서 즉시 자동 파싱한다.** 사용자가 Ctrl+V 하는 순간 반응해야 한다. 별도 "분석" 버튼을 두지 말 것.
3. 파싱 결과에 따라 상태 라벨을 갱신:
   - 형식 불일치 → `인식할 수 없는 텍스트입니다. 헤더(###WSCH1)가 포함됐는지 확인하세요.`
   - 체크섬 불일치 → `복사 중 텍스트가 잘렸거나 변형됐습니다. 다시 복사해 주세요.`
   - 파트 일부만 도착 → `2/3 조각까지 받았습니다. 남은 조각 1개를 이어서 붙여넣으세요.` **이때 TextBox 를 자동으로 비워** 다음 조각을 바로 붙여넣게 한다. 받은 조각은 모듈 수준 버퍼에 누적.
   - 전부 도착 → 미리보기 목록에 각 건을 `[신규] / [갱신] / [변화없음] / [삭제]` 로 표시하고 `[반영]` 활성화
4. `[반영]` → 병합 실행 → `신규 3건, 갱신 1건, 변화없음 2건 반영했습니다.` 요약 표시 → 달력·오늘 시트 자동 갱신
5. 같은 조각을 두 번 붙여넣어도 안전해야 한다(멱등).

## 병합 규칙

| 상황 | 처리 |
|---|---|
| 처음 보는 ID | 신규 추가 |
| 기존 ID, 들어온 `수정시각`이 더 최신 | 덮어쓰고 log_Merge 에 이전값 기록 |
| 기존 ID, 들어온 `수정시각`이 더 과거이거나 동일 | 무시 |
| 들어온 `삭제여부=1` | 내 쪽도 삭제 플래그 (수정시각 비교보다 우선) |
| 문서번호 중복이지만 ID가 다름 | 병합은 하되 완료 요약에 `문서번호 중복 1건` 으로 경고 |

2~5명 규모다. 3-way 자동 머지 같은 과설계 금지. **최신 우선 + 충돌 시 알려주기**로 충분하다.

# UI 설계 원칙

> **입력은 UserForm, 조회·목록은 시트에 그린다.** 이 원칙을 어기지 말 것.

UserForm 은 취소선·회색처리·수십 줄 스크롤 전부 번거롭다. 시트는 셀 서식만 바꾸면 되고 인쇄도 된다.

## 보이는 시트 3개

### `달력` 시트
- 7열 × 6행 그리드를 병합셀+테두리로 미리 그려두고 VBA 는 텍스트만 채운다. 컨트롤 42개 동적 생성 금지.
- 각 칸 표시:
  ```
  27
  업무 3 · 연락 1
  공유 2
  ```
- 건수 집계는 각 표를 한 번씩만 순회하며 `Scripting.Dictionary` 에 날짜별로 누적 → 한 번에 렌더링. 셀마다 COUNTIFS 금지.
- 오늘 날짜 칸은 배경 강조. 회신기한이 3일 이내인 공유건이 있는 날은 빨간 표식.
- 상단에 `◀ 2026년 8월 ▶` 이동 버튼.
- 날짜 칸 클릭(`Worksheet_SelectionChange`) → 클릭 위치에서 날짜 역산 → `오늘` 시트를 그 날짜로 렌더링하고 이동.

### `오늘` 시트
상단에 조회 날짜 표시 + `[◀][오늘][▶]`. 아래로 3개 섹션.

**1) 업무**
- A열에 `☐` / `☑` 문자, B열에 제목, C열에 시간.
- A열 클릭(`SelectionChange`) → 토글. 완료 시 해당 행 전체에 `Font.Strikethrough = True`, `Font.Color` 회색 적용, `tbl_Task` 의 완료여부·완료시각 갱신.
- B열(제목) 클릭 → 내용 팝업(`frmDetail`). 내용이 200자 미만이면 `MsgBox` 로도 무방.
- 시간 있는 항목 먼저, 시간순. 시간 없는 항목은 그 뒤에 등록순.

**2) 유관부서 연락하기**
- `이름(부서) | 내선 1234 | 14:00` 형태.
- 내선번호는 저장된 값이 아니라 `이름+부서`로 명부를 실시간 조회한 결과. 명부에 없으면 `내선캐시` 값, 그것도 없으면 `(명부 없음)`.
- 완료 체크박스는 업무와 동일 방식.

**3) 업무공유**
- `문서번호 | 제목 | D-3 | 회신기한 08/30 | 작성자` 형태.
- D-day = `회신기한 - Date`. 0 이면 `D-DAY` 굵게 빨강, 음수면 `D+2` 빨강, 3 이하면 주황.
- 제목 클릭 → 내용 팝업.
- 우상단에 `[공유 내보내기]` `[공유 가져오기]` 버튼.

### `설정` 시트
- 내 이름(동기화 시 작성자로 기록됨), 명부 파일 경로, `[명부 갱신]` 버튼, 백업 보관일수, 마지막 동기화 시각, `[전체 데이터 내보내기]` `[전체 데이터 가져오기]`.

## 입력 폼

`frmTask` / `frmContact` / `frmShare` / `frmLunch` 각각 등록·수정 겸용.

### `frmContact` 의 이름 검색 — 동명이인 처리
```
사용자가 "지은" 입력 (또는 일부만)
  ↓
mst_Employee 를 Variant 배열로 한 번에 읽음 (Range.Value2)
  ↓
InStr() 부분일치 검색
  ↓
0건 → "명부에 없습니다. 이름과 내선번호를 직접 입력하시겠습니까?"
1건 → 팝업 없이 자동 세팅 (이름·부서·내선 즉시 표시)
2건+ → frmPicker 팝업, ListBox 4열 [이름 | 부서 | 직급 | 내선] → 선택
```
**성능**: 셀을 한 칸씩 `Cells(i,1).Value` 로 읽으면 수백 배 느리다. 반드시 `Range.Value2` 로 배열 통째 읽기. 명부가 5000행을 넘으면 이름→행번호 Dictionary 인덱스를 만들어 모듈 변수에 캐시하고, 명부 갱신 시 무효화.

### `frmLunch` 의 충돌 검사
저장 직전 두 가지를 확인하고 `MsgBox vbYesNo` 로 계속 진행 여부를 묻는다:
1. 같은 날짜에 이미 다른 약속 → `8/27에 김철수님과 약속이 있습니다. 그래도 등록할까요?`
2. 같은 사람 + 같은 날짜 → 중복 경고

# 반드시 넣을 것

- **자동 백업**: `Workbook_Open` 에서 `백업\Scheduler_yyyymmdd.xlsm` 로 복사. 같은 날 이미 있으면 건너뜀. 보관일수 초과분 자동 삭제. VBA 통합 문서는 실제로 손상되는 일이 있어 이건 선택이 아니다.
- **전체 데이터 내보내기/가져오기**: 프로그램을 v2 로 올릴 때 데이터 이관 통로. 처음부터 만들어 둘 것.
- **`Application.ScreenUpdating = False`**: 모든 렌더링 함수 앞뒤. 이거 하나로 체감 속도가 몇 배 달라진다.
- **에러 핸들러**: 모든 Public 프로시저에 `On Error GoTo ErrHandler` + 종료부에서 `ScreenUpdating`/`EnableEvents` 를 반드시 복구. 이걸 빠뜨리면 에러 한 번에 화면이 멈춘 채로 남는다.
- **`Application.EnableEvents` 관리**: `SelectionChange` 안에서 셀을 쓰면 재귀 호출된다. 반드시 끄고 켤 것.
- **`Option Explicit`**: 모든 모듈 최상단.

## 알림 기능에 대해
`Application.OnTime` 으로 지정 시각 팝업이 가능하지만 **엑셀이 열려 있을 때만** 동작한다. README 에 이 제약을 명시하고, 엑셀이 닫혀 있어도 알림이 필요하면 Windows 작업 스케줄러 연동이 필요하며 사내 PC 정책 확인이 선행되어야 한다고 안내할 것. 구현은 하지 않는다.

# 구현 순서

이 순서를 지켜라. 뒤 단계가 앞 단계 구조에 의존한다.

1. `modUtil` + `modData` — ID 생성, 이스케이프, 체크섬, 표 CRUD. **여기에 대한 테스트 프로시저를 먼저 만들고 통과시킬 것.**
2. `build.ps1` 로 빈 xlsm 생성 + 시트/표 세팅이 되는지 확인
3. `frmTask` + `오늘` 시트 업무 섹션 + 체크박스 완료 토글
4. `modEmployee` 명부 로드·검색 + `frmPicker` + `frmContact`
5. `modCalendar` 달력 렌더링 + 날짜 클릭 연동
6. `frmLunch` (가장 단순)
7. **`modSync` + `frmExport` + `frmImport`** — 마지막. 앞 구조가 잡혀야 병합이 깔끔해진다.

# 검증

`modTest.bas` 에 아래를 확인하는 프로시저를 만들고 전부 통과시킨 뒤 완료 보고할 것:

1. 이스케이프 왕복: `내용|파이프\백슬래시` + 줄바꿈 + 탭이 들어간 문자열이 인코딩→디코딩 후 원본과 정확히 일치
2. `\\p` 처럼 이스케이프 문자가 연속된 악성 입력에서 파싱이 깨지지 않음
3. 체크섬이 한 글자 변형을 잡아냄
4. 3파트로 분할된 데이터를 **역순으로** 붙여넣어도 정상 병합됨
5. 같은 파트를 두 번 붙여넣어도 중복 생성되지 않음(멱등)
6. 수정시각이 더 과거인 레코드가 최신 레코드를 덮어쓰지 않음
7. 삭제 플래그가 켜진 레코드가 병합 후 되살아나지 않음
8. 명부에 동명이인 3명이 있을 때 팝업이 3건을 보여주고, 1명뿐이면 팝업 없이 자동 세팅
9. 명부 내선번호 `0212` 가 `212` 로 변형되지 않음
10. 부서·직급 열이 완전히 비어 있는 명부로도 전체 기능이 동작

# README.md 에 반드시 포함

한국어로 작성하고 다음을 담을 것:

1. **신뢰할 수 있는 위치 등록** — Excel → 옵션 → 보안 센터 → 보안 센터 설정 → 신뢰할 수 있는 위치. 팀원 전원이 1회 설정해야 매크로가 차단되지 않는다.
2. **VBA 프로젝트 개체 모델 액세스 신뢰** — 빌드 스크립트를 돌릴 사람만 필요. 배포받는 사람은 불필요.
3. **명부 준비 방법** — 양식 CSV 를 채워서 팀에 공유 → 각자 `[명부 갱신]`.
4. **공유 주고받는 절차** — 내보내기 → 메신저 붙여넣기 → 받는 사람 가져오기. 조각이 여러 개일 때의 순서.
5. **주의**: 업무·연락·점심은 각자 PC 로컬 데이터이며 공유되지 않는다. 공유되는 건 업무공유뿐이다.
6. **백업 폴더 위치와 복구 방법.**

# 작업 방식

- 각 단계마다 실제로 빌드해서 동작을 확인한 뒤 다음으로 넘어간다. 전부 작성하고 마지막에 한 번에 테스트하지 말 것.
- VBA 코드 주석은 한국어로.
- 사용자에게 보이는 모든 문구(버튼, 메시지, 오류)는 한국어. 존댓말.
- 명세가 모호한 지점을 발견하면 임의로 정하지 말고 물어볼 것. 단, 이미 명시된 것을 다시 묻지는 말 것.
