# 내부망 로컬 업무 스케줄러 — 아키텍처 설계

**전제**: 망분리 내부망 / 공유폴더 없음 / 엑셀·VBA만 사용 가능 / 사용자 2~5명 / DB 없음

**결론**: 5개 기능 전부 구현 가능. 단 3번(업무공유)만 실시간이 아닌 **비동기 병합 방식**이 된다.

---

## 1. 전체 구조

단일 매크로 통합 문서 `Scheduler.xlsm` 하나가 앱 전체다. DB 대신 **숨김 시트를 테이블로** 쓴다.

```
Scheduler.xlsm
├─ [UI 시트]  (보이는 시트)
│   ├─ 달력        ← 월간 요약 달력 (7×6 그리드)
│   ├─ 오늘        ← 오늘의 할일 대시보드 (3개 섹션)
│   └─ 설정        ← 내 이름/ID, 명부 경로, 공유 폴더 경로
│
├─ [데이터 시트] (숨김, ListObject 표)
│   ├─ tbl_Task      업무
│   ├─ tbl_Contact   연락하기
│   ├─ tbl_Share     업무공유
│   ├─ tbl_Lunch     점심약속
│   └─ mst_Employee  직원 명부 (이름/부서/내선) ← 외부 엑셀에서 복사해옴
│
└─ [VBA 모듈]
    ├─ modData     시트 CRUD (Add/Update/Find)
    ├─ modCalendar 달력 렌더링
    ├─ modToday    오늘의할일 렌더링
    ├─ modEmployee 명부 검색 (이름 → 내선번호)
    ├─ modSync     공유 내보내기/가져오기/병합  ★핵심
    ├─ frmTask / frmContact / frmShare / frmLunch  입력 폼
    └─ frmPicker   동명이인 선택 팝업 / 내용 보기 팝업
```

### 설계 원칙 하나만 기억하면 됨
> **입력은 UserForm, 조회·목록은 시트에 그린다.**

UserForm은 취소선·회색처리·수십 줄 스크롤이 전부 번거롭다. 시트는 셀 서식만 바꾸면 끝이고 인쇄도 된다.

---

## 2. 데이터 스키마

모든 표에 공통으로 들어가는 4개 컬럼이 병합의 핵심이다.

| 컬럼 | 설명 |
|---|---|
| `ID` | `홍길동-20260827-143052-8471` 형태 문자열. **자동증가 숫자 쓰면 안 됨** (PC마다 따로 증가해서 충돌) |
| `작성자` | 설정 시트의 내 이름 |
| `수정시각` | 병합 시 최신 우선 판정용 |
| `삭제여부` | 물리 삭제 금지. 플래그만 켬 (지운 게 병합 때 되살아나는 걸 막음) |

### tbl_Task (업무)
`ID, 제목, 내용, 날짜, 시간(선택), 완료여부, 완료시각, 작성자, 수정시각, 삭제여부`
- 시간 미입력 → 빈 값. 정렬 시 시간 없는 항목은 뒤로.
- 날짜 ≠ 오늘 → 자동으로 그 날짜 할일로 들어감 (별도 처리 불필요, 달력이 날짜로 필터링만 함)

### tbl_Contact (연락하기)
`ID, 사번, 이름, 날짜(선택), 시간(선택), 완료여부, 작성자, 수정시각, 삭제여부`
- ⚠️ **이름이 아니라 `사번`(고유키)을 저장**한다. 내선번호는 저장하지 않고 조회할 때마다 명부에서 끌어온다 → 명부 갱신하면 전화번호가 자동으로 최신화됨.

### tbl_Share (업무공유)
`ID, 문서번호, 제목, 내용, 등록일, 회신기한, 작성자, 수정시각, 삭제여부`
- 문서번호는 필수 + 중복 검사.

### tbl_Lunch (점심약속)
`ID, 대상이름, 날짜, 시간(선택), 장소, 작성자, 수정시각, 삭제여부`

### mst_Employee (명부)
`사번, 이름, 부서, 직급, 내선번호`
- 사용자가 주는 엑셀을 그대로 쓰지 않고 **"명부 갱신" 버튼**으로 외부 파일을 열어 이 시트에 복사해온다. (외부 링크 수식은 파일 경로 바뀌면 다 깨짐)
- 사번 컬럼이 원본에 없으면 `이름+부서`를 키로 대체.

---

## 3. 기능별 구현 방법

### 기능 2 — 이름으로 전화번호 찾기 (동명이인 팝업)

```
사용자가 "지은" 입력
  ↓
mst_Employee 전체를 Variant 배열로 한 번에 읽음 (Range.Value)
  ↓
InStr()로 이름 부분일치 검색 — 수천 행도 0.1초 이내
  ↓
결과 0건 → "명부에 없습니다. 직접 입력할까요?"
결과 1건 → 자동 세팅 (팝업 안 뜸)
결과 2건+ → frmPicker 팝업, ListBox에 [이름 | 부서 | 직급 | 내선] 표시
  ↓
선택 → 사번을 tbl_Contact에 저장
```

**성능 주의**: 셀을 한 칸씩 `Cells(i,2).Value`로 읽으면 수백 배 느리다. 반드시 배열로 통째로 읽을 것. 명부가 1만 행을 넘어가면 Dictionary로 이름→행번호 인덱스를 만들어 캐시.

---

### 기능 3 — 요약 달력

시트에 7열 × 6행 그리드를 **미리 그려두고**(병합셀 + 테두리), VBA가 각 칸의 텍스트만 채운다.

```
각 날짜 칸:
  27              ← 날짜 (윗줄)
  업무 3 · 연락 1  ← 건수 (아랫줄, 작은 글씨)
  공유 2
```

- 건수는 `tbl_*`를 날짜별로 한 번 순회하며 Dictionary에 집계 → 한 방에 렌더링.
- 오늘 날짜는 배경색 강조, 회신기한 임박한 공유건 있으면 빨간 점.
- 칸 클릭 → `Worksheet_SelectionChange` 이벤트에서 클릭 위치 → 날짜 역산 → "오늘" 시트를 그 날짜로 렌더링하거나 팝업.

**UserForm에 달력을 그리지 말 것.** 컨트롤 42개 동적 생성은 느리고 코드가 지저분해진다.

---

### 기능 4 — 오늘의 할일 (시트 대시보드)

"오늘" 시트를 3개 섹션으로 나눠 VBA가 매번 다시 그린다.

**1) 업무 — 체크박스 + 취소선**

체크박스 구현 방법 3가지 중 권장안:

| 방식 | 평가 |
|---|---|
| ListView 컨트롤 | ❌ `mscomctl.ocx` 참조 필요 → 다른 PC에서 깨짐. 배포 환경에서 금지 |
| UserForm 동적 CheckBox | △ 가능하나 20건 넘으면 스크롤 처리가 지저분 |
| **시트 셀 렌더링** | ✅ **권장.** A열에 `☐`/`☑` 문자, B열에 제목 |

```
A열 클릭 → Worksheet_BeforeDoubleClick 또는 SelectionChange
  ☐ → ☑ 로 바꾸고
  같은 행에 Font.Strikethrough = True, Font.Color = 회색
  tbl_Task의 완료여부 = True, 완료시각 기록
```

제목 셀 클릭 → 내용 팝업(`frmDetail`). 내용이 짧으면 MsgBox로도 충분.

**2) 유관부서 연락하기**

`이름(부서) | 내선 1234 | 14:00` 형태로 한 줄씩. 내선번호는 저장값이 아니라 사번으로 명부 실시간 조회한 결과.

**3) 업무공유**

`문서번호 | 제목 | D-3 | 회신기한 08/30`
- D-day = `회신기한 - Date`. 음수면 `D+2` 빨간색, 0이면 `D-DAY` 강조.
- 제목 클릭 → 내용 팝업.

---

### 기능 5 — 점심약속 충돌 안내

저장 직전 검사 2가지:
1. 같은 날짜에 이미 약속 존재 → "8/27에 김철수님과 약속이 있습니다. 계속할까요?"
2. 같은 사람 + 같은 날짜 중복 → 중복 등록 경고

---

## 4. ★ 업무공유를 남들이 보게 하는 방법

> **확정**: 보안정책상 방안 A(파일공유) 불가, 메신저 첨부도 불가로 확인됨. → **방안 B의 "텍스트 복붙" 변형으로 확정.** 상세 프로토콜은 `CLAUDE_CODE_프롬프트.md` 참고. 아래 A·C는 기록용.

### 방안 A — PC 간 공유폴더를 직접 만든다 (❌ 이번 건은 불가)

"공유폴더가 없다"와 "공유폴더를 만들 수 없다"는 다르다. 같은 내부망 LAN에 있는 PC끼리라면 한 대를 허브로 정해 폴더 공유를 걸 수 있는 경우가 많다.

```
허브 PC의 C:\Share 폴더 우클릭 → 공유 → 팀원 계정에 읽기/쓰기 권한
→ 다른 PC에서 \\허브PC이름\Share 로 접근
```

**IT팀에 확인할 것 딱 하나**: "PC 간 SMB 파일공유(445 포트)가 보안정책상 허용되나요?"

허용되면 구조가 이렇게 단순해진다.

```
\\허브PC\Share\
   ├─ share_data.xlsx      ← 공유 업무만 담긴 데이터 파일
   ├─ share_data.lock      ← 쓰는 중 표시 (있으면 대기)
   └─ employee.xlsx        ← 명부도 여기 두면 갱신 일원화
```

- 읽기: 각자 Scheduler.xlsm이 열릴 때 `share_data.xlsx`를 읽기전용으로 열어 tbl_Share에 로드
- 쓰기: `.lock` 파일 생성 → 추가 → lock 삭제. 2~5명이면 이 단순 잠금으로 충분
- 잠금 파일에 시각을 기록해 5분 넘으면 강제 해제(비정상 종료 대비)

### 방안 B — 파일 조각 교환 (공유폴더가 불가능할 때)

망분리 환경의 정석. 실시간은 포기하고 확실히 동작하는 걸 택한다.

```
[내보내기]
  "공유 내보내기" 버튼
  → 내가 등록한 공유건만 추출
  → share_홍길동_20260827.csv 생성 (또는 클립보드에 텍스트 복사)
  → 사내 메일/메신저로 팀원에게 전달

[가져오기]
  받은 csv를 폴더에 모아두고 "가져오기" 버튼
  → 폴더 내 csv 전부 스캔
  → ID 기준 병합:
       처음 보는 ID       → 새로 추가
       이미 있는 ID       → 수정시각 큰 쪽 채택 (last-write-wins)
       삭제여부=True      → 내 쪽도 삭제 처리
  → "3건 추가, 1건 갱신" 결과 요약
```

**메신저 친화 옵션**: 파일 첨부가 막혀 있으면 CSV 대신 **한 덩어리 텍스트**를 생성해 클립보드에 넣어주고, 받는 쪽은 "붙여넣기로 가져오기" 버튼을 쓴다. 구분자는 `|`처럼 흔한 문자 말고 `¶` 같은 걸 쓰거나 값에 이스케이프 처리.

이 방식이 사용자가 말한 *"파일을 가진 사람 누구나 입력 가능하며 내용은 다같이 보여짐"* 요구와 정확히 맞는 모델이다.

### 방안 C — Outlook 자동화

사내 메일이 Outlook이고 VBA에서 Outlook 객체 모델 접근이 허용되면, 내보내기/받기를 자동화할 수 있다. 다만 보안정책으로 차단된 경우가 많아 **A·B가 안 될 때만** 검토.

---

## 5. 충돌 처리 규칙 (2~5명 기준)

| 상황 | 처리 |
|---|---|
| 서로 다른 사람이 다른 건 등록 | ID가 달라 자연히 공존 |
| 같은 건을 두 사람이 수정 | `수정시각` 최신 채택. 진 쪽 내용은 로그 시트에 남겨 확인 가능하게 |
| 한쪽이 삭제, 한쪽이 수정 | 삭제 우선 (tombstone) |
| 문서번호 중복 | 등록 시점에 경고 + 병합 시 리스트업 |

2~5명 규모에선 자동 3-way 머지 같은 건 과한 설계다. **"최신 우선 + 충돌 났으면 알려주기"**로 충분하고, 애매하면 사람이 판단하는 게 빠르다.

---

## 6. 운영상 반드시 넣어야 할 것

- **자동 백업**: 파일 열 때마다 `backup\Scheduler_20260827.xlsm` 복사. VBA 통합 문서는 손상 사례가 실제로 있다. 30일치 보관 후 자동 삭제.
- **신뢰할 수 있는 위치**: 매크로 차단 안 되게 파일 폴더를 Excel 옵션 → 보안 센터 → 신뢰할 수 있는 위치에 등록. 팀원 전원 1회 설정 필요.
- **전체 데이터 내보내기/가져오기**: 나중에 프로그램을 v2로 올릴 때 데이터 이관 통로. 처음부터 만들어두면 나중에 고생 안 함.
- **`Application.ScreenUpdating = False`**: 렌더링 함수 앞뒤로 필수. 이거 하나로 체감 속도가 몇 배 달라진다.
- **에러 핸들러**: 모든 public 프로시저에 `On Error GoTo` + 화면 갱신 복구. 안 하면 에러 한 번에 화면이 멈춘 채로 남는다.

### 알림(스케줄러) 기능에 대해
`Application.OnTime`으로 지정 시각에 팝업을 띄울 수 있지만 **엑셀이 열려 있어야만** 동작한다. 엑셀이 꺼져 있어도 알림이 필요하면 윈도우 작업 스케줄러로 엑셀을 자동 실행하는 방법이 있으나, 사내 PC 정책 확인이 필요하다.

---

## 7. 만드는 순서 (권장)

1. 데이터 시트 + `modData` CRUD — 여기가 튼튼하면 나머지는 쉽다
2. `frmTask` 업무 등록 + "오늘" 시트 렌더링 + 체크박스 완료 처리
3. 명부 로드 + 이름 검색 + 동명이인 팝업 → 연락하기 기능
4. 달력 렌더링 + 날짜 클릭 연동
5. 점심약속 (가장 단순)
6. **업무공유 + 병합** — 마지막. 앞의 구조가 잡혀야 병합 로직이 깔끔해진다

---

## 8. 지금 확인이 필요한 것

1. **IT팀**: PC 간 SMB 파일공유가 허용되는가? → 예면 방안 A로 훨씬 쉬워짐
2. **명부 엑셀 샘플**: 컬럼 구성, 사번 유무, 행 수, 동명이인 실제 빈도
3. 사내 메신저/메일로 파일 첨부 주고받기가 가능한가? (방안 B의 전제)
