# 작업 기준 파일

수정하다 만 사본이 여러 개 있어서, 어느 것이 기준인지 여기 적어 둔다.

## 기준

```
dist/배포/sched.html          ← 이것 하나만 고친다
```

사본을 만들지 않는다. 전에 `web/스케줄러.html` 에 같은 내용이 한 벌 더 있었는데,
한쪽만 고치면 조용히 어긋나서 지웠다. 되살리려면 `git show f626967:web/스케줄러.html` 로 꺼낸다.

`build/` 의 검증 스크립트 7개도 전부 이 경로를 읽는다.

## 기준이 아닌 것

| 파일 | 무엇인가 |
|---|---|
| `dist/AX금융혁신팀_업무스케줄러_20260827_v3/업무스케줄러.html` | 2시간 앞선 판. 기준본이 이것의 확장이다 |
| `web/스케줄러_개편전.html` | 개편 전 화면 |
| `build/.fixture/design_v2.html` · `design_v3.html` | 디자인 시안 |

## v3 → 기준본에서 무엇이 늘었나

`+375 / -59` 줄. **사라진 함수가 하나도 없다** — 지우고 다시 쓴 것이 아니라 얹은 것이다.

새로 붙은 함수 6개는 전부 어딘가에서 불린다 (떠 있는 코드가 아니다).

| 함수 | 호출되는 곳 |
|---|---|
| `paintSetSave` | 5곳 |
| `meDirty` · `shareCfgDirty` | 각 4곳 |
| `shareSpanDays` | 2곳 |
| `dayPeekModal` · `sharePark` | 각 1곳 |

구조도 성한 상태다 — `</html>` 로 제대로 끝나고, 스크립트 중괄호가 634 대 634 로 맞는다.
미완 표시(`XXX` 1개, "나중에" 5개)는 v3 에도 똑같이 있던 것이라 이번에 생긴 것이 아니다.
